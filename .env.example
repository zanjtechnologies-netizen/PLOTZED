# ========================================
# Plotzed Environment Variables Template
# ========================================
#
# This is a template file for developers.
# Copy this to .env.local and fill in your values.
#
# For production deployment to Vercel:
# - See VERCEL_DEPLOYMENT_GUIDE.md
# - Use .env.production template
# - Set variables in Vercel Dashboard
#
# ========================================

# ========================================
# Database Configuration
# ========================================

# For local development (PostgreSQL)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/plotzed_db?schema=public"

# For cloud development (Neon PostgreSQL)
# Get from: https://console.neon.tech/
# DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require"
# DIRECT_DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require"

# ========================================
# NextAuth Configuration
# ========================================

NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-generate-with-openssl-rand-base64-32"

# OAuth Providers (Optional - for Google/Facebook sign-in)
# Google OAuth: https://console.cloud.google.com/apis/credentials
# - Create OAuth 2.0 Client ID
# - Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Facebook OAuth: https://developers.facebook.com/apps
# - Create App > Add Facebook Login product
# - Valid OAuth Redirect URIs: http://localhost:3000/api/auth/callback/facebook
FACEBOOK_CLIENT_ID="your-facebook-app-id"
FACEBOOK_CLIENT_SECRET="your-facebook-app-secret"

# ========================================
# Encryption & JWT Tokens
# ========================================

# Generate with: openssl rand -hex 32
ENCRYPTION_KEY="your-encryption-key-generate-with-openssl-rand-hex-32"

# Generate with: openssl rand -base64 64
ACCESS_TOKEN_SECRET="your-access-token-secret"
REFRESH_TOKEN_SECRET="your-refresh-token-secret"

# ========================================
# File Storage (Cloudflare R2)
# ========================================

# Get from: https://dash.cloudflare.com/
# Create R2 bucket: "plotzed" or "plotzed-storage"
R2_ENDPOINT="https://your-account-id.r2.cloudflarestorage.com"
R2_ACCESS_KEY_ID="your-r2-access-key-id"
R2_SECRET_ACCESS_KEY="your-r2-secret-access-key"
R2_BUCKET="plotzed"
R2_ACCOUNT_ID="your-account-id"
CLOUDFLARE_R2_TOKEN="your-r2-api-token"
CLOUDFLARE_R2_PUBLIC_URL="https://pub-your-subdomain.r2.dev"

# ========================================
# Caching & Rate Limiting (Upstash Redis)
# ========================================

# Get from: https://console.upstash.com/
# Create a Redis database (free tier available)
UPSTASH_REDIS_REST_URL="https://your-redis.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# ========================================
# Email Service (Gmail)
# ========================================

# Get App Password from: https://myaccount.google.com/apppasswords
# 1. Enable 2FA on your Google account
# 2. Generate App Password (16 characters)
EMAIL_USER="your-email@gmail.com"
EMAIL_PASSWORD="your-gmail-app-password"  # 16 chars, no spaces
EMAIL_FROM="Your App Name <your-email@gmail.com>"
ADMIN_EMAIL="admin@yourdomain.com"

# ========================================
# WhatsApp Business API (Optional)
# ========================================

# Get from: https://business.facebook.com/
# Follow: WHATSAPP_SETUP_GUIDE.md for complete setup
WHATSAPP_ENABLED="false"  # Set to "true" after setup
WHATSAPP_PHONE_NUMBER_ID=""  # From Meta Business Manager
WHATSAPP_ACCESS_TOKEN=""  # Permanent access token
WHATSAPP_BUSINESS_ACCOUNT_ID=""  # Optional
WHATSAPP_VERIFY_TOKEN=""  # Optional for webhooks

# ========================================
# Error Tracking (Sentry)
# ========================================

# Get from: https://sentry.io/
# Create a new Next.js project
SENTRY_DSN="https://your-key@o123456.ingest.sentry.io/123456"
NEXT_PUBLIC_SENTRY_DSN="https://your-key@o123456.ingest.sentry.io/123456"

# For local builds with source maps (optional)
# SENTRY_AUTH_TOKEN="your-sentry-auth-token"

# ========================================
# Google reCAPTCHA v3 (Recommended)
# ========================================

# Get from: https://www.google.com/recaptcha/admin
# Register a v3 site with:
# - Domain: localhost (development) or your-domain.com (production)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY="your-recaptcha-site-key"
RECAPTCHA_SECRET_KEY="your-recaptcha-secret-key"
RECAPTCHA_MIN_SCORE="0.5"  # 0.0-1.0 (0.5 recommended)

# Test keys (always return score 0.0):
# NEXT_PUBLIC_RECAPTCHA_SITE_KEY="6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI"
# RECAPTCHA_SECRET_KEY="6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe"

# ========================================
# Security & API
# ========================================

# Generate with: openssl rand -base64 32
CRON_SECRET="your-cron-secret-key"

# API Key Authentication (optional in development)
REQUIRE_API_KEY="false"  # Set to "true" in production
API_KEYS="your-key-1,your-key-2"  # Comma-separated list

# ========================================
# Feature Flags
# ========================================

# Payment & Booking Features (Currently Disabled)
FEATURE_PAYMENTS_ENABLED="false"
FEATURE_PLOT_BOOKING_ENABLED="false"

# Site Visits Feature (Active)
FEATURE_SITE_VISITS_ENABLED="true"

# ========================================
# Optional Services
# ========================================

# Google Maps API
# Get from: https://console.cloud.google.com/
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# Google Analytics
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# ========================================
# PAYMENT INTEGRATION - DISABLED
# ========================================
# Razorpay credentials are not required since payments are disabled
# When you enable payments, uncomment and fill these:
# NEXT_PUBLIC_RAZORPAY_KEY_ID="rzp_test_xxxxxxxxxxxxx"
# RAZORPAY_KEY_SECRET="your-razorpay-key-secret"
# RAZORPAY_WEBHOOK_SECRET="your-webhook-secret"

# ========================================
# Development Notes
# ========================================
#
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Generate secrets:
#    openssl rand -base64 32  # For NEXTAUTH_SECRET, CRON_SECRET
#    openssl rand -hex 32     # For ENCRYPTION_KEY
#    openssl rand -base64 64  # For ACCESS_TOKEN_SECRET, REFRESH_TOKEN_SECRET
#
# 3. Required for basic functionality:
#    - DATABASE_URL
#    - NEXTAUTH_SECRET
#    - ENCRYPTION_KEY
#    - ACCESS_TOKEN_SECRET
#    - REFRESH_TOKEN_SECRET
#    - R2_* (file storage)
#    - UPSTASH_* (rate limiting)
#    - EMAIL_* (notifications)
#
# 4. Optional but recommended:
#    - SENTRY_DSN (error tracking)
#    - RECAPTCHA_* (bot protection)
#    - WHATSAPP_* (messaging)
#
# 5. For production deployment:
#    - See VERCEL_DEPLOYMENT_GUIDE.md
#    - Use .env.production template
#    - Generate NEW secrets (don't reuse dev secrets)
#
# ========================================
