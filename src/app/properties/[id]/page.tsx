// ================================================
// src/app/properties/[id]/page.tsx - Property Detail Page (Alias)
// ================================================

/**
 * This is an alias for /plots/[id]
 * Both /plots/[id] and /properties/[id] show the same content
 * This provides flexibility in URL structure
 */

export { default } from '@/app/plots/[id]/page'
